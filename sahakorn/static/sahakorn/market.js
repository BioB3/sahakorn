import { universalFetcher } from './utils.js'


const filterBtn = {}
// btnName: actualBtn

async function populateMarketTable(marketItems) {
  for (marketItem of marketItems) {
    // append to table
    // another for loop in here
    // if there's a filter that's not here yet, make new btn
  }
}

function generateBtn(name) {

}

document.addEventListener('DOMContentLoaded', async () => {

})